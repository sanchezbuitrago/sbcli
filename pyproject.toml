[build-system]
requires = [
    "setuptools>=61.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "sbcli"
version = "0.1.0"
description = "CLI para gestión de tareas"
dependencies = [
    "typer[all]",
    "requests==2.31.0"
]

[project.scripts]
sbcli = "sbcli.main:app"